#include <iostream>
#include <cmath>
#include <vector>

using namespace std;

int main(){
    
    ios::sync_with_stdio();
    cin.tie(0);
    cout.tie(0);

    unsigned long long n;
    cin >> n;    
    
    cout << n * (unsigned long long) 3;
}