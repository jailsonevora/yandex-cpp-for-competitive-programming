#include <iostream>
#include <string>
#include <algorithm>

using namespace std;

void maximal(long long &){

}

int main(){

    ios::sync_with_stdio(false);
    cin.tie(0);
    long long n = 9;
    maximal(n);
    cout << n;
}